# Gradle daemon heap and UTF-8 file encoding
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# use AndroidX libraries instead of the old Support Library
android.useAndroidX=true
# official Kotlin formatting style
kotlin.code.style=official
# each module R class contains only its own resources
android.nonTransitiveRClass=true
